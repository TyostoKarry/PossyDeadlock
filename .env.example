# Bot token — Discord Developer Portal → Your App → Bot → Token → Reset Token
DISCORD_TOKEN=

# Application ID — Discord Developer Portal → Your App → General Information → Application ID
CLIENT_ID=

# Environment — 'development' or 'production'
# Use 'development' in .env.dev, 'production' in .env
ENVIRONMENT=